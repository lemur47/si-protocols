# OSV-Scanner configuration
# Lockfiles (uv.lock, site/package-lock.json, site-cc/package-lock.json)
# are auto-discovered via --recursive scanning.
#
# To ignore a vulnerability, add:
# [[IgnoredVulns]]
# id = "GHSA-xxxx-yyyy-zzzz"
# reason = "Not applicable because ..."

[[IgnoredVulns]]
id = "GHSA-5239-wwwm-4pmq"
reason = "Low-severity pygments issue (CVSS 3.3), no fix available upstream"
